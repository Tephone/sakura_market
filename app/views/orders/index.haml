%h2 #{current_user.name}さんの購入履歴
%table
  %thead
    %tr
      %th 注文日
      %th product名
      %th 注文個数
  %tbody
    - @orders.each do |o|
      -o.order_products.each do |op|
        %tr
          %td= op.created_at.strftime('%Y/%m/%d')
          %td= op.product.name
          %td= op.amount
= link_to "戻る", products_path


